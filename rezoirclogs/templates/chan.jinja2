{% extends "base.jinja2" %}

{% block title %}{{ chan.__name__ }}{% endblock %}

{% block content %}
<h1>{{ chan.__name__ }}</h1>

    {% if chan.__parent__ %}
        <a href="{{ chan.__parent__ | model_url }}">..</a>
    {% endif %}

    <p><a href="{{ context|model_url('search') }}">Search</a></p>

    {% for file in chan %}
        {% if loop.first %}
            <h2>Dates</h2>
            <div id="main">
            <ul>
        {% endif %}

    <div class=file><li> <a href="{{ file | model_url }}">{{ file.__name__ }}</a></div>

        {% if loop.last %}
            </ul>
        {% endif %}
    {% endfor %}
    </div>
{% endblock %}
